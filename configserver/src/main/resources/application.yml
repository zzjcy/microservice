server:
  port: 8015
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zzjcy/microservice
          search-paths: respo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/